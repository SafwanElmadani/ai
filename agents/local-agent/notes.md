
## local agent with Ollama, LangChain & RAG

- for RAG, we will use Chroma database.
- requirements:
```sh
pip install langchain
pip install langchain-ollama
pip install langchain-chroma
pip install pandas
```
